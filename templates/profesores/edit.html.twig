{% extends 'base.html.twig' %}

{% block title %}Editar Profesores{% endblock %}

{% block body %}
<div class = "container p-4">
    <h1>Editar Profesores</h1>
    <table>
        {{ include('profesores/_form.html.twig', {'button_label': 'Guardar'}) }}

        <a href="{{ path('app_profesores_index') }}"><button type="button" class="btn btn-primary">Volver a la Lista</button></a>

        {{ include('profesores/_delete_form.html.twig') }}
    </table>    
</div>
{% endblock %}
